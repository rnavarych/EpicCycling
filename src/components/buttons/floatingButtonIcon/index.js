import React from 'react';
import { View } from 'react-native';
import styles from './styles';

function FloatingButtonIcon({style, icon}) {
  return (
    <View style={[{position: 'absolute'}, style]}>

    </View>
  )
}

export default FloatingButtonIcon;